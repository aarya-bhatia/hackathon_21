import React from "react";

const Recipe = ({
  label,
  image,
  instructions,
  url,
  dietLabels,
  totalTime,
  mealTypes,
  calories,
  ingredients,
}) => {
  return <div>{label}</div>;
};

export default Recipe;
